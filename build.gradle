// Top-level build file — DO NOT add allprojects{} here
// repositories are in settings.gradle → dependencyResolutionManagement
buildscript {
    dependencies {
        classpath 'com.android.tools.build:gradle:8.2.2'
    }
}
